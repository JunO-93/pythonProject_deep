# 함수 고급
```buildoutcfg
* 일급 객체
    - 데이터처럼 사용이 가능하다
    - 매개변수에 넘겨 줄 수 있다.
    - 리턴값으로 사용될 수 있다.
    
    ✔ 파이썬에서 함수도 일급 객체가 될 수 있다.
    
* 클로저
    - 내부 함수란 무엇일까?
        # 함수 안에서 정의되는 함수
    - 클로저란?
        # 함수가 종료되얻 자원을 사용할 수 있는 함수 (자원 = 변수)
    - 클로저가 될 3가지 조건
        # 내부함수여야 한다.
        # 외부 함수의 변수를 참조해야한다.
        # 외부 함수가 내부 함수를 반환해야 한다.
        
* 이터레이터

* 제너레이터

* 데코레이터

```